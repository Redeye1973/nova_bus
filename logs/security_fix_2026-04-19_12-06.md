# NOVA v2 Security Fix Rapport

**Datum**: 2026-04-19T12:06:00Z  
**Duur**: ca. 10–15 minuten (geschat)

## SSH Key Setup

- [x] Key gegenereerd/aanwezig (`~/.ssh/id_ed25519`)
- [x] Public key op Hetzner geplaatst in `authorized_keys`
- [x] Key-based login getest (`KEY_WORKS`)

## Root Password Rotatie

- [x] Nieuw password gegenereerd (cryptografisch random, lokaal)
- [x] Oud password vervangen op server via `chpasswd` (sessie: key-based)
- [x] Nieuw password opgeslagen in `L:\!Nova V2\secrets\nova_v2_passwords.txt`
- [x] Oud password (uit chat) gemarkeerd als INVALID in secrets file (comment)

## SSH Daemon Hardening

- [x] Backup van `sshd_config` (extra kopie: `sshd_config.backup_fix` — eerste backup via PowerShell had een fout in de datum in de bestandsnaam)
- [x] `PasswordAuthentication no`
- [x] `PermitRootLogin prohibit-password`
- [x] `PubkeyAuthentication yes`
- [x] `systemctl reload ssh` / `sshd` — succesvol
- [x] Password-only login geverifieerd geweigerd (`BatchMode=yes` → Permission denied)

## Documentatie Update

- [x] `infrastructure/README.md` — MinIO-sectie (19000/19001)
- [x] `infrastructure/.env.template` — commentaar + poortnotities
- [x] `README.md` (projectroot) — Access URLs met MinIO :19001

## UFW Firewall

- [x] Status: **enabled**
- [x] Regels toegepast: 22, 5678, 5679, 5680, 19001 (+ bestaande regels 8090, 3000, 9001 bleven staan van eerdere setup)

## Status van services na fix (extern getest)

- V1 (5678): HTTP 200
- V2 main (5679): HTTP 200
- V2 webhook (5680): HTTP 404 (verwacht zonder webhook-pad)
- MinIO console (19001): HTTP 200

## Action items voor jou

- Nieuw root-wachtwoord alleen uit `secrets\nova_v2_passwords.txt` halen indien nodig (niet in chat).
- Backup van `id_ed25519` + `.pub` op een veilige plek (offline of wachtwoordmanager).
- Password authentication staat uit op de server — alleen SSH-key voor root.

## Waarschuwing

Eenmalig root-wachtwoord is gebruikt om de eerste public key te plaatsen; daarna is het root-wachtwoord geroteerd. Het oude wachtwoord is ongeldig.
