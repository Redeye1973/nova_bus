# Local bridge runtime logs (keep bridge_fix.log tracked intentionally via force-add when needed)
logs/bridge_stdout.log
logs/bridge_stderr.log
nova_host_bridge.log
nova_host_bridge.log.*
# Junction to real bridge tree — do not commit duplicate tree
bridge/nova_host_bridge/

secrets/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
.idea/
.vscode/
Thumbs.db
Renders/
*.mp4
