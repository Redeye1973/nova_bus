# 20260419_001 - Batch 2: Agents 11, 17, 21

**Van**: claude.ai  
**Naar**: Cursor  
**Prioriteit**: normal  
**Expected duration**: 2-3 uur

## Context

Baseline verified (zie `shared_state/current_baseline.md`):
- 4 echte agents operationeel: 01, 02, 10, 20
- 29 stubs healthy, klaar om gevuld te worden
- Infrastructure stabiel (N8n + Postgres + Redis + MinIO + Qdrant)

Dit is de eerste handoff via het nieuwe bridge system.

## Doel

Vul 3 stubs met echte logica volgens hun Pipeline Build prompts: Monitor (11), Error (17), FreeCAD Parametric (21).

## Opdracht

1. **Agent 11 Monitor**
   - Lees: `L:\!Nova V2\pipelines\nova_v2_pipeline_build\nova_v2_pipeline_build\01_fase1_foundation\prompt_09_agent_11_monitor.md`
   - Vervang stub logic in `nova-v2-agent-11` met volle Monitor implementatie
   - Monitor moet: lezen uit docker socket + Hetzner docker ps output + agent status files + formatteren overzicht
   - Rebuild image + redeploy container
   - Test webhook met `GET /webhook/monitor`
   - Update `L:\!Nova V2\status\agent_11_status.json` met schema zoals agent_20
   - Git commit: "agent 11: monitor full implementation"
   - Run `python utils\agent_validator.py --agent 11`

2. **Agent 17 Error**
   - Lees: `prompt_10_agent_17_error.md`
   - Vervang stub met volle Error Agent logica (PostgreSQL historie, pattern grouping, suggested fixes)
   - Rebuild + redeploy
   - Test: POST met simulate flag `{"agent":"test","error_type":"timeout","simulate":true}`
   - Update status JSON + git commit + validator

3. **Agent 21 FreeCAD Parametric**
   - Lees: `prompt_04_agent_21_freecad_parametric.md`
   - Vereist FreeCAD op host (PC) - check `nova_config.yaml` voor path
   - Container roept FreeCAD aan via SSH/subprocess
   - **Als FreeCAD niet geïnstalleerd**: implementeer fallback mode die placeholder STEP files returnt, markeer `fallback_mode=true` in status JSON met `reason="freecad_not_installed"`. Ga door, niet blokkeren.
   - Test: POST met eenvoudige ship component spec
   - Update status + commit + validator

## Constraints

- **V1 niet aanraken** (poort 5678, 68 productieworkflows)
- **Sprite Jury 01 niet aanraken** (POC draait)
- Max 3 pogingen per substap, dan docs raadplegen
- Bij blocker op één agent: documenteer, ga naar volgende, niet blijven hangen
- Status JSONs moeten schema `webhook_url, agent_number, name, status, fallback_mode, built_at, tests_passed` volgen (zie agent_20 als referentie)

## Success criteria

- Minimaal 2 van 3 agents active status (21 mag fallback zijn als FreeCAD ontbreekt)
- `utils\agent_validator.py --agent XX` groen voor elke
- Git has 3 commits (één per agent)
- Sprite Jury 01 nog werkend na deployments (regression check)
- V1 nog werkend (curl check op 5678)

## Rapporteer terug

Schrijf naar `handoff/from_cursor/20260419_001_response.md` volgens template in `templates/response_template.md`.

Update `shared_state/current_baseline.md` met:
- Agents 11, 17, 21 naar "actieve agents" sectie (indien geslaagd)
- Stubs lijst verkleinen
- Eventuele nieuwe known issues

Als iets blokkeert: update `shared_state/blockers.md` met format uit template.

## Notities voor Cursor

- Agent 11 Monitor is relatief simpel (leesbewerkingen + formatting)
- Agent 17 Error vereist PostgreSQL schema voor error historie - check of table al bestaat, anders create
- Agent 21 FreeCAD kan tricky zijn vanwege host dependency - focus op werkende fallback, volle functionaliteit kan later als FreeCAD geïnstalleerd
- Na succes: aanbeveling in response voor batch 3 (suggestie: agents 22 Blender, 23 Aseprite, 25 PyQt5 - rest van Fase 1)
